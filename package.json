{
  "name": "boilerplate",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @repo/shared build && pnpm --filter @repo/config build && pnpm -r --parallel --filter api --filter web run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}